rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /guide-files/{uid}/{folder}/{file} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
      allow read: if false;
      allow write: if false;
      allow delete: if false;
    }
  }
}
